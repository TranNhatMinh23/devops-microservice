
replicaCount: 1

image:
  repository: 0914818516/ui
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

service:
  type: NodePort
  port: 8080
  targetport: 8080

container:
  name: react-ui